# Fonts for PDF export

Place a Cyrillic-capable TrueType font here for the route PDF export:

- `Montserrat-Regular.ttf`
- `Montserrat-SemiBold.ttf`

Download from Google Fonts (Montserrat). The PDF route embeds these explicitly
so Cyrillic renders correctly (default PDF fonts do NOT support Cyrillic).
